/**
 * Creator :
 * Axoux & Creepy
*/

#include "algoLib.hpp"

int main(void) {
    t_data data;
    if (data.bld.setValueInDeck() == false)
        return (EXIT_FAILURE);
    return (EXIT_SUCCESS);
}
